// /components/StoreInitializer.tsx
'use client'
import { useEffect } from 'react'

/**
 * StoreInitializer was used to seed mock data.
 * Since the application is now fully integrated with Supabase,
 * we no longer need to seed initial data in the client.
 */
export function StoreInitializer() {
  // Mock seeding disabled to ensure live data from Supabase is used exclusively.
  return null
}
